# Rotina XP

Aplicação web local (React + TypeScript + Vite) para acompanhamento de rotina gamificada pessoal com persistência em `localStorage`.

## Rodar localmente

```bash
npm install
npm run dev
```

## Testes

```bash
npm run test
```
